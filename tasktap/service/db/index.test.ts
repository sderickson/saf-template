import { describe, it, expect } from "vitest";
import * as exports from "@your-org/tasktap-db";

describe("@your-org/tasktap-db", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
