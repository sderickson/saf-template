import { describe, it, expect } from "vitest";
import * as exports from "@your-org/tasktap-identity";

describe("@your-org/tasktap-identity", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
