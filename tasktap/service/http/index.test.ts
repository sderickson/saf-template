import { describe, it, expect } from "vitest";
import * as exports from "@your-org/tasktap-http";

describe("@your-org/tasktap-http", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
