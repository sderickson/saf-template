import { describe, it, expect } from "vitest";
import * as exports from "@your-org/tasktap-service-common";

describe("@your-org/tasktap-service-common", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
