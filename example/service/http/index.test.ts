import { describe, it, expect } from "vitest";
import * as exports from "@your-org/example-http";

describe("@your-org/example-http", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
