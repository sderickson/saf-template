import { describe, it, expect } from "vitest";
import * as exports from "@your-org/example-service-common";

describe("@your-org/example-service-common", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
