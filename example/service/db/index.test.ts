import { describe, it, expect } from "vitest";
import * as exports from "@your-org/example-db";

describe("@your-org/example-db", () => {
  it("should be defined", () => {
    expect(exports).toBeDefined();
  });
});
