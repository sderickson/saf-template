{
  "name": "@your-username/clients",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "format": "prettier --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "ts:check": "vue-tsc -b"
  },
  "dependencies": {
    "@saflib/vue-spa": "*"
  },
  "devDependencies": {
    "@saflib/vue-spa-dev": "*"
  }
}